<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, create_time, update_time, create_user, update_user, status)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{status})
    </insert>

    <update id="startOrStop">
            update dish set status = #{status},
            update_time = #{updateTime},
            create_user = #{createUser},
            update_user = #{updateUser},
            create_time = #{createTime}
            where id = #{id}
    </update>

    <update id="update">
            update dish set name = #{name}, category_id = #{categoryId}, price = #{price},
            image = #{image}, description = #{description},
            update_time = #{updateTime}, update_user = #{updateUser},
            status = #{status} where id = #{id}
    </update>
    <delete id="delete">
        delete from dish where id in
       <foreach collection="ids" item="dish" open="(" separator="," close=")">
        #{dish}
       </foreach>
    </delete>

    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select * , category.name as categoryName from dish  left join category on dish.category_id = category.id
        <where>
            <if test="name != null and name != ''">
                dish.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and dish.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and dish.status = #{status}
            </if>
        </where>
    </select>

</mapper>
