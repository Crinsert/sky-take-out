<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCarMapper">
    <insert id="insert">
        insert into shopping_cart
            (user_id, dish_id, setmeal_id, dish_flavor,
             number, amount, create_time)
        values
            (#{userId}, #{dishId}, #{setmealId},
             #{dishFlavor}, #{number}, #{amount}, #{createTime})
    </insert>
    <delete id="deleteByUserId">
            delete from shopping_cart where user_id = #{userId}
    </delete>
    <delete id="deleteByStemealIdAndDishId">
        delete from shopping_cart
        <where>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
        </where>
    </delete>


    <select id="selectByDishOrSetmeal" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>
</mapper>

